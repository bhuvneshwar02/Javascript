// use if to execute a block of code if a specified condition is true
// use else to execute a block of code if the same condition is false
// use else if to specify a new condition to test, if the first condition is false
// use switch to specify many alternative blocks of code to be executed
// */
/*
// wether the number is even or odd
 let num = 10;
 if(num % 2 === 0){
     console.log('Number is even'); 
 }

 // wwether the number is positive or negative

    let num1 = -10;
    if(num1 > 0){
        console.log('Number is positive');
    }
    else{
        console.log('Number is negative');
    }

    //print grade of student , if marks is greater than 90 then grade is A , if marks is greater than 80 then grade is B , if marks is greater than 70 then grade is C and below this is fail

    let marks = 75;
    let grade;
    if(marks > 90){
        grade ='A';
    } else if(marks > 80){
        grade = 'B';
    }
    else if(marks > 70){
        grade = 'C';
    }
    else{
        grade = 'Fail';
    }
    console.log('Grade is',grade);
    */